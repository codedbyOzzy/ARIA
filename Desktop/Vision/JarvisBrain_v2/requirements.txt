# Core MCP / HTTP / config
fastmcp
httpx
python-dotenv
ddgs

# Voice agent (LiveKit)
livekit-agents[deepgram,groq,openai,sarvam,silero]>=1.5.1
livekit-plugins-google>=1.5.1

# Desktop UI / window automation
pyautogui
pygetwindow
PySide6

# STT / TTS / audio I/O
SpeechRecognition
pyttsx3
sounddevice
numpy
edge-tts
pygame
faster-whisper      # offline local Whisper STT (internet gerektirmez)

# System / clipboard / vision
psutil
pyperclip

# Windows-only audio + audio mixer control
pyaudio; platform_system == "Windows"
pycaw; platform_system == "Windows"
